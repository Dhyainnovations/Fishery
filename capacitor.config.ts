import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fishery.app',
  appName: 'Fishery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
